# Shared

Future home for **cross-package contracts** (e.g. OpenAPI-generated TypeScript types, JSON schemas, or shared constants).

The API (`api/`) and launcher (`launcher/`) remain separate packages; anything placed here should be **buildable and versioned** with the repo.
